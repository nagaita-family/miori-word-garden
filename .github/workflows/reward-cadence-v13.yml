name: Reward cadence v13

on:
  push:
    paths:
      - '.github/workflows/reward-cadence-v13.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Apply faster reward cadence
        run: python3 tools/reward-cadence-v13.py
      - name: Validate JavaScript
        run: node --check app.js
      - name: Check reward thresholds
        run: |
          grep -q "id:'bench',xp:30" app.js
          grep -q "id:'seedcrate',xp:300" app.js
          grep -q "wordsAway" app.js
      - name: Commit app update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.js
          git diff --cached --quiet && exit 0
          git commit -m "Speed up garden reward cadence"
          git push
